#include<bits/stdc++.h>
using namespace std;

int main()
{
    list<int> ls;

    ls.push_back(23);           // 23
    ls.emplace_back(24);        // 23, 24
    ls.push_front(21);          // 21, 23, 24
    


    // rest of the functions are like vector
}
